# Astro frontend profile

This directory is owned by the `astro` frontend overlay for `$project_name`.
The Hayate backend remains in `src/`; the site will consume its explicit API
contract in static or hybrid mode.

The executable Astro profile is added by its frontend-profile milestone.
