# React frontend profile

This directory is owned by the `react` frontend overlay for `$project_name`.
The Hayate backend remains in `src/`; the browser application will consume its
explicit API contract.

The executable React profile is added by its frontend-profile milestone.
